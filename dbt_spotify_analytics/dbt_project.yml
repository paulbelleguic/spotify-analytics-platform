name: spotify_analytics
version: '1.0.0'
config-version: 2

profile: spotify_analytics

model-paths: ["models"]

models:
  spotify_analytics:
    staging:
      +schema: staging
      +materialized: view
    marts:
      +schema: analytics
      +materialized: table
      reporting:
        +schema: analytics
        +materialized: view
