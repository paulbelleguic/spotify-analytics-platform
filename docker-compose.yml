services:
  postgres:
    image: postgres:16
    container_name: spotify_analytics_postgres
    environment:
      POSTGRES_DB: spotify_analytics
      POSTGRES_USER: spotify
      POSTGRES_PASSWORD: spotify
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
